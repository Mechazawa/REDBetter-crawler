usage: whatupload [-h] [-u USERNAME] [-p PASSWORD] [--config CONFIG]
                  release_url flac_dir

positional arguments:
  release_url           the URL where the release is located
  flac_dir              the location of the FLAC files

optional arguments:
  -h, --help            show this help message and exit
  -u USERNAME, --username USERNAME
                        your username on What.CD
  -p PASSWORD, --password PASSWORD
                        your password on What.CD
  --config CONFIG       the location of the configuration file (default:
                        ~/.whatupload)
